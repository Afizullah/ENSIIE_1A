#include "liste.h"
#include <stdlib.h>
#include <stdio.h>


int main(){




/*	liste l = liste_vide();
	affiche(l);
	printf("%d\n",taille(l));
	
	if (est_vide(l)==1) printf("la liste est vide\n");
	
	else{ printf("la liste n'est pas vide\n"); }
	l=insert(2,l);
	l=insert(3,l);
	affiche(l);
	int v = head(l);
	printf("%d\n",v);
	l = pop(l);
	affiche(l);
*/
	
	return 0;
};
